# ENTITY application agent route

Read `ENTITY_CONTEXT.json` before substantive ENTITY work. It identifies the governed knowledge repository, pinned context revision and this application's inspected baseline. Locate a checkout containing that exact knowledge revision; do not replace it with an arbitrary latest branch or this conversation's memory.

From the knowledge checkout, run its `working/engineering-context/context.py entry --route component --binding <absolute path to this repository>/ENTITY_CONTEXT.json --repo application=<absolute path to this repository>`. Read the shared context manifest and the task's relevant governing sources. A binding failure requires source/revision reconciliation, not bypassing the check.

The pointer supplies orientation. It does not authorise implementation. Before code changes, use the existing approved component work package with exact application revision, requirements, recorded decisions and rationale, interfaces/shared constraints, acceptance cases, source bindings, resource limit, rollback, exception triggers and controlled write-back destination. Check that package with the same context tool. READY_FOR_REVIEW is not implementation or release permission.

Preserve settled decisions and distinguish proposals, historical observations and current runtime evidence. Investigate missing rationale before asking Navraj; never invent a prior decision. Record material decisions and reasons in their existing governed destinations during work and return one source-bound completion handback.

Preserve user changes. Inspect startup, migration, seed and test paths before running them. Use only the isolated resources/data and costs authorised by the package. No production changes, queue resume, destructive reset, deployment, publication, new paid service or paid model run is authorised by this pointer. Retain the configured Bedrock and human review/release boundaries. Do not modify agent instructions or tests merely to bypass a failed check.
